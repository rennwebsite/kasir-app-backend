{
  "name": "kasir-app-backend",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "start": "json-server --watch coffe.json --port 3001"
  }
}
